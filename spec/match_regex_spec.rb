require "spec_helper"

describe MatchRegex do
  it "has a version number" do
    expect(MatchRegex::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
